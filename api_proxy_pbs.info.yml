name: PBS Schedule API Proxy
description: 'Proxy module providing legacy API compatibility for PBS mobile apps using Drupal JSON:API backend.'
core_version_requirement: ^9 || ^10 || ^11
package: "PBS"
type: module
version: "2.0.0"
dependencies:
  - drupal:system
configure: api_proxy_pbs.settings
